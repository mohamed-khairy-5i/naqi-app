<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naqi.app">

    <!-- أذونات Android الأساسية -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- أذونات الكاميرا والتقاط الشاشة -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_OUTPUT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- أذونات النافذة العائمة -->
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />
    <uses-permission android:name="android.permission.DISMISS_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- أذونات الحماية والأمان -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    
    <!-- أذونات إدارة التطبيقات -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    
    <!-- أذونات الموقع (اختيارية) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- أذونات التخزين -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- أذونات أخرى -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISMISS_KEYGUARD" />

    <application
        android:label="تطبيق نقي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/LaunchTheme"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">

        <!-- النشاط الرئيسي -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- يخفي الشعار الأولي -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- خدمة التقاط الشاشة -->
        <service
            android:name=".NaqiScreenService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- مستقبل إشعارات الإقلاع -->
        <receiver android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.POWER_ON" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- مستقبل إشعارات النوافذ العائمة -->
        <receiver android:name=".OverlayReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.naqi.app.OVERLAY_PERMISSION" />
            </intent-filter>
        </receiver>

        <!-- مستقبل إشعارات إدارة الطاقة -->
        <receiver android:name=".AlarmManagerReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.naqi.app.ALARM_ACTION" />
            </intent-filter>
        </receiver>

        <!-- موفر محتوى الملف -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/network_security_config" />
        </provider>

        <!-- إعدادات الأمان -->
        <meta-data
            android:name="android.net.http.maxErrorResponseBodySize"
            value="10485760" />
            
        <!-- إعدادات الذكاء الاصطناعي -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            value="face_detection,image_labeling,text_recognition" />

        <!-- إعدادات الأداء -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            value="false" />

        <!-- إعدادات الأمان المتقدمة -->
        <meta-data
            android:name="com.naqi.app.SECURITY_LEVEL"
            value="advanced" />
            
        <!-- معلومات الإصدار -->
        <meta-data
            android:name="com.naqi.app.VERSION"
            value="1.0.0" />
            
        <!-- إعدادات الترجمة -->
        <meta-data
            android:name="com.naqi.app.LANGUAGE"
            value="ar,en" />

    </application>
</manifest>